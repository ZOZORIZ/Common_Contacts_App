<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- res/values/themes.xml -->
    <style name="Base.Theme.AndroidPlayground" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="android:windowIsTranslucent">true</item>
    </style>




    <style name="Theme.Camera" parent="android:Theme.Material.Light.NoActionBar"/>
    <style name="Theme.Gallery" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/white</item>
    </style>
    <style name="CircularImageShape">
        <item name="shapeAppearanceOverlay">@style/CircleShape</item>
    </style>

    <style name="CircleShape">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50dp</item>
    </style>
    <style name="splash_screen_theme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
    </style>


    <style name="Theme.MyApp.Splash.FullScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Ensures the splash screen covers the entire screen -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

</resources>